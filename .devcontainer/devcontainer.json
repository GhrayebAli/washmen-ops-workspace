{
  "name": "Washmen Ops Workspace",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",

  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/devcontainers/features/sshd:1": { "version": "latest" }
  },

  "runArgs": ["--cap-add=NET_ADMIN", "--device=/dev/net/tun"],

  "forwardPorts": [4000, 3000, 1339, 2339],
  "portsAttributes": {
    "4000": { "label": "vibe-ui", "onAutoForward": "silent", "visibility": "public" },
    "3000": { "label": "ops-frontend", "onAutoForward": "silent", "visibility": "public" },
    "1339": { "label": "internal-public-api", "onAutoForward": "silent", "visibility": "public" },
    "2339": { "label": "srv-internal-user-backend", "onAutoForward": "silent" }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y jq openvpn && sudo corepack enable && corepack prepare yarn@3.2.4 --activate && bash .devcontainer/setup-vpn-config.sh; bash core/setup.sh",
  "postStartCommand": "bash .devcontainer/start-openvpn.sh; bash core/start.sh",

  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        ".devcontainer/extensions/workspace-layout"
      ],
      "settings": {
        "workbench.startupEditor": "none",
        "workbench.activityBar.visible": false,
        "editor.minimap.enabled": false
      }
    }
  },

  "secrets": {
    "ANTHROPIC_API_KEY": {
      "description": "Anthropic API key for Claude Agent SDK"
    },
    "WASHMEN_GITHUB_TOKEN": {
      "description": "GitHub PAT with repo scope for cloning and pushing"
    },
    "GOOGLE_MAPS_KEY": {
      "description": "Google Maps API key for REACT_APP_GOOGLE_SERVICES_MAPS_KEY"
    },
    "ALGOLIA_APP_ID": {
      "description": "Algolia application ID"
    },
    "ALGOLIA_API_KEY": {
      "description": "Algolia search-only API key"
    },
    "SENTRY_DSN": {
      "description": "Sentry DSN for error reporting"
    },
    "E2E_CLIENT_ID": {
      "description": "Cognito E2E client ID for dev iframe machine auth"
    },
    "E2E_CLIENT_SECRET": {
      "description": "Cognito E2E client secret for dev iframe machine auth"
    },
    "VPN_PRIVATE_KEY": {
      "description": "Private key for AWS Client VPN (PEM format, base64-encoded)"
    },
    "VIEWS_PGPASSWORD": {
      "description": "PostgreSQL password for washmen_views database"
    },
    "REDSHIFT_WAREHOUSE_DB_PASSWORD": {
      "description": "Redshift warehouse database password"
    },
    "INTERNAL_USER_AUTH_SALT": {
      "description": "Salt for internal user auth password hashing"
    },
    "GOOGLE_PLACES_API_KEY": {
      "description": "Google Places API key for reviews"
    },
    "INTERCOM_ACCESS_TOKEN": {
      "description": "Intercom API access token"
    },
    "AWS_ACCESS_KEY_ID": {
      "description": "AWS access key for DynamoDB, S3, SNS"
    },
    "AWS_SECRET_ACCESS_KEY": {
      "description": "AWS secret key for DynamoDB, S3, SNS"
    }
  },

  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb"
  },

  "containerEnv": {
    "WORKSPACE_DIR": "/workspaces/washmen-ops-workspace"
  }
}
